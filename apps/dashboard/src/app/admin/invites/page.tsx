export default function InvitesPage() {
  return (
    <section>
      <h1>Invites &amp; join codes</h1>
      <p style={{ color: "var(--muted)", maxWidth: "44rem" }}>
        Generate and revoke 6-digit join codes that let a new guard or
        supervisor redeem a membership scoped to the right site. New accounts
        land as <code>org_admin</code>; everyone else joins by code. Lands in
        M5.
      </p>
    </section>
  );
}
