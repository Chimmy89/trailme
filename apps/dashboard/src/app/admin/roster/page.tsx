export default function RosterPage() {
  return (
    <section>
      <h1>Roster</h1>
      <p style={{ color: "var(--muted)", maxWidth: "44rem" }}>
        Manage <code>memberships</code>: who belongs to the org, their role, and
        which sites they are scoped to. Live presence surfaces who is online plus
        their battery level and tracking health, so a control room sees a guard
        about to go dark. Lands in M5.
      </p>
    </section>
  );
}
